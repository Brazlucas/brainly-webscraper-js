/** @type {import('next').NextConfig} */
const nextConfig = {
  serverExternalPackages: ["brainly-scraper-v2", "puppeteer", "puppeteer-extra", "puppeteer-extra-plugin-stealth"],
};

export default nextConfig;
